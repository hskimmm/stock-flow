<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.stockflow.mapper.inbound.InboundMapper">
    <select id="getInbounds" resultType="inbound">
        SELECT id, inbound_no, product_id, product_name, product_code, inbound_date, quantity, supplier, remarks, reg_user_id, reg_user_name, reg_date
        FROM v_inbound_list
        ORDER BY inbound_date DESC
        LIMIT #{skip}, #{amount}
    </select>

    <select id="getInbound" resultType="inbound">
        SELECT id, inbound_no, product_id, product_name, product_code, inbound_date, quantity, supplier, remarks, reg_user_id, reg_user_name, reg_date
        FROM v_inbound_list
        WHERE id = #{id}
    </select>

    <select id="getProducts" resultType="product">
        SELECT id, product_name, current_stock, unit
        FROM product
        WHERE del_yn = 'N'
    </select>

    <select id="findLastInboundCodeByDate" resultType="String">
        SELECT inbound_no
        FROM inbound
        WHERE inbound_no LIKE CONCAT('IN-', #{dateStr}, '%')
        ORDER BY inbound_no DESC
        LIMIT 1
    </select>

    <insert id="addInbound">
        INSERT INTO inbound
        (
            inbound_no,
            product_id,
            inbound_date,
            quantity,
            supplier,
            remarks,
            reg_user_id
        )
        VALUES
        (
            #{inboundNo},
            #{productId},
            #{inboundDate},
            #{quantity},
            #{supplier},
            #{remarks},
            #{regUserId}
        )
    </insert>

    <select id="getTotalInbound" resultType="int">
        SELECT COUNT(id)
        FROM inbound
        WHERE id > 0
    </select>
</mapper>
