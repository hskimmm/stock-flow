<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.stockflow.mapper.product.ProductMapper">
    <select id="getProducts" resultType="product">
        SELECT id, product_code, product_name, category, unit, safety_stock, current_stock, remarks, reg_date, mod_date, del_yn
        FROM product
        WHERE del_yn = 'N'
    </select>
</mapper>